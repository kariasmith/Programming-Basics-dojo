// Creating a variable named likeCount and set to value of 0.
var likeCount = 0;
// Creating funtion named increaseLikes
function increaseLikes() {
    // Increasing the likeCount value by 1
    likeCount = likeCount + 1;
}
